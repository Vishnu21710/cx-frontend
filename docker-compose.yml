services:
  cx-frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000/api}
    image: cx-frontend:latest
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
